{
  "variables": {
    "arch": "64",
    "cpus": "1",
    "memory": "2048",
    "disk_size": "40960",
    "box_basename": "rhel7",
    "boot_time": "10s",
    "build_timestamp": "{{isotime \"20060102150405\"}}",
    "git_revision": "__unknown_git_revision__",
    "headless": "",
    "http_proxy": "{{env `http_proxy`}}",
    "https_proxy": "{{env `https_proxy`}}",
    "iso_checksum": "03f3a0291634335f6995534d829bd21ffaa0d000004dfeb1b2fb81052d64a4d5",
    "iso_checksum_type": "sha256",
    "iso_name": "rhel-server-7.2-x86_64-dvd.iso",
    "ks_path": "rhel-7.2/ks.cfg",
    "metadata": "floppy/dummy_metadata.json",
    "mirror": "home/thomas/vms",
    "mirror_directory": "isos",
    "name": "rhel-7.2",
    "no_proxy": "{{env `no_proxy`}}",
    "template": "rhel-7.2-x86_64",
    "version": "2.2.TIMESTAMP"
  },
  "builders": [
    {
      "boot_command": [
        "<tab> text ks=http://{{ .HTTPIP }}:{{ .HTTPPort }}/{{user `ks_path`}}<enter><wait>"
      ],
      "type": "virtualbox-iso",
      "boot_wait": "{{user `boot_time`}}",
      "disk_size": "{{user `disk_size`}}",
      "guest_additions_path": "VBoxGuestAdditions_{{.Version}}.iso",
      "guest_os_type": "RedHat_64",
      "hard_drive_interface": "sata",
      "headless": "{{ user `headless` }}",
      "http_directory": "http",
      "iso_checksum": "{{user `iso_checksum`}}",
      "iso_checksum_type": "{{user `iso_checksum_type`}}",
      "iso_url": "{{user `mirror`}}/{{user `mirror_directory`}}/{{user `iso_name`}}",
      "output_directory": "packer-{{user `template`}}-virtualbox",
      "shutdown_command": "echo 'thomas' | sudo -S /sbin/halt -h -p",
      "ssh_password": "thomas",
      "ssh_port": 22,
      "ssh_username": "thomas",
      "ssh_wait_timeout": "10000s",
      "vboxmanage": [
        [
          "modifyvm",
          "{{.Name}}",
          "--memory",
          "{{ user `memory` }}"
        ],
        [
          "modifyvm",
          "{{.Name}}",
          "--cpus",
          "{{ user `cpus` }}"
        ]
      ],
      "virtualbox_version_file": ".vbox_version",
      "vm_name": "{{ user `template` }}"
    },
    {
      "boot_command": [
        "<tab> text ks=http://{{ .HTTPIP }}:{{ .HTTPPort }}/{{user `ks_path`}}<enter><wait>"
      ],
      "type": "vmware-iso",
      "boot_wait": "{{user `boot_time`}}",
      "disk_size": "{{user `disk_size`}}",
      "guest_os_type": "rhel7-64",
      "headless": "{{ user `headless` }}",
      "http_directory": "http",
      "iso_checksum": "{{user `iso_checksum`}}",
      "iso_checksum_type": "{{user `iso_checksum_type`}}",
      "iso_url": "{{user `mirror`}}/{{user `mirror_directory`}}/{{user `iso_name`}}",
      "output_directory": "packer-{{user `template`}}-vmware",
      "shutdown_command": "echo 'thomas' | sudo -S /sbin/halt -h -p",
      "ssh_password": "thomas",
      "ssh_port": 22,
      "ssh_username": "thomas",
      "ssh_wait_timeout": "10000s",
      "tools_upload_flavor": "linux",
      "vm_name": "{{ user `template` }}",
      "vmx_data": {
        "cpuid.coresPerSocket": "1",
        "memsize": "{{ user `memory` }}",
        "numvcpus": "{{ user `cpus` }}"
      }
    },
    {
      "boot_command": [
        "<tab> text ks=http://{{ .HTTPIP }}:{{ .HTTPPort }}/{{user `ks_path`}}<enter><wait>"
      ],
      "type": "qemu",
      "boot_wait": "{{user `boot_time`}}",
      "disk_size": "{{user `disk_size`}}",
      "headless": "{{ user `headless` }}",
      "http_directory": "http",
      "iso_checksum": "{{user `iso_checksum`}}",
      "iso_checksum_type": "{{user `iso_checksum_type`}}",
      "iso_url": "{{user `mirror`}}/{{user `mirror_directory`}}/{{user `iso_name`}}",
      "output_directory": "packer-{{user `template`}}-qemu",
      "shutdown_command": "echo 'thomas'|sudo -S /sbin/halt -h -p",
      "ssh_password": "thomas",
      "ssh_port": 22,
      "ssh_username": "thomas",
      "ssh_wait_timeout": "10000s",
      "vm_name": "{{ user `template` }}"
    }
  ],
  "post-processors": [
    {
      "type":          "vsphere",
      "vm_name":       "basebox",
      "host":          "phovc03.fbfs.com",
      "username":      "todd.thomas@fblfinancial.com",
      "password":      "flbAug16",
      "datacenter":    "HomeOffice",
      "cluster":       "Cluster4",
      "resource_pool": "/HomeOffice/host/Cluster4/Resources",
      "datastore":     "HO_NA_SSD_SCCMTEMPLATES",
      "vm_folder":     "linux/devpoc/templates",
      "vm_network":    "THO_PRESENTATION1",
      "overwrite":     true,
      "insecure":      true,
      "options": [
				"--computerName", "base",
				"--diskMode",     "thin"
			]
    }
  ],
  "provisioners": [
    {
      "destination": "/tmp/bento-metadata.json",
      "source": "{{user `metadata`}}",
      "type": "file"
    },
    {
      "environment_vars": [
        "HOME_DIR=/home/thomas",
        "http_proxy={{user `http_proxy`}}",
        "https_proxy={{user `https_proxy`}}",
        "no_proxy={{user `no_proxy`}}"
      ],
      "execute_command": "echo 'thomas' | {{.Vars}} sudo -S -E sh -eux '{{.Path}}'",
      "scripts": [
        "scripts/common/metadata.sh",
        "scripts/rhel/networking.sh",
        "scripts/common/sshd.sh",
        "scripts/common/sysadmin.sh",
        "scripts/rhel/config-rhn-subscript-man.sh",
        "scripts/rhel/cleanup.sh",
        "scripts/common/minimize.sh"
      ],
      "type": "shell"
    }
  ]
}
